// Fill this in after you deploy the tablet-checkin/ folder to Hostinger.
// It must match CHECKIN_API_KEY in config.php exactly (see README.md).
window.CHECKIN_CONFIG = {
  apiKey: 'REPLACE_ME_WITH_THE_SAME_KEY_AS_CONFIG_PHP',
};
